apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-postgresql
  labels:
{{ include "hotload-integration-tests.labels" . | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app: postgresql
{{ include "hotload-integration-tests.selectorLabels" . | indent 4 }}